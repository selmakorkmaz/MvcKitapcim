@model IEnumerable<Kitapcim.Models.Yonetici>
@{
    ViewBag.Title = "Store";
}
<h3>Browse Genres</h3>

<p>
    Select from @Model.Count() genres:
</p>
<ul>
    @foreach (var a in Model)
    {
        <li>@Html.ActionLink(a.tur, "Browse", new { a= a.tur })</li>
    }
</ul>
